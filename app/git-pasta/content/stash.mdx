## Stash

```bash
# Stash current changes
git stash

# List stack order of stashed changes
git stash list

# Apply stashed changes from the top of the stack
git stash pop

# Discard stashed changes from the top of the stack
git stash drop
```
